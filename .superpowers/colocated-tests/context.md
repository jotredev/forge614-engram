# Approved correction: colocated tests

User approved: each implementation file with logic has its own sibling <basename>.test.ts. Tests target that file's behavior; executing it indirectly from somebody else's test is not its own coverage. Collaboration tests go in owning functionality's __tests__/<behavior>.integration.test.ts; import implementations, never other test suites. Root tests/ retains only genuine system-wide e2e/architecture and shared fixtures. Pure type declarations/re-export barrels/static data do not need artificial tests. Tests may use real dependencies; no global module mocks that contaminate other files. Preserve every existing behavioral case and resource cleanup. Do not claim 100% branch coverage just because each file has a test.

Existing modular refactor all uncommitted on refactor/modular-architecture; preserve it. No commits/staging/push/new branch. No production behavior changes, no personal config/database access. Use temp fixtures. Real PostgreSQL fixture uses FORGE614_TEST_POSTGRES_BIN=/tmp/engram-postgres-17.6.tTVxxc/postgres/bin. No docs/es/docs/en/Notion; controller updates handoff. No subagents from workers.

Ownership:
- infrastructure worker: src/infrastructure sibling tests/support/integration plus original tests/integration/{store,projects,shared,retrieval,sessions,sync,workspace-config,assistant-config,postgres-sync}.test.ts and tests/unit/postgres-options.test.ts. Classify legacy facade suites as app collaboration if appropriate, but write these into src/app/__tests__ with distinct original basenames; other worker will not touch them. Preserve originalcasecoverage; use direct adapter calls in own siblingtests. Root tests/e2e/postgres-sync remains global unchanged.
- application worker: src/app,src/modules,src/shared sibling tests/support/integration; original tests/integration/{workspace,setup,project-context,public-api}.test.ts and tests/unit/{sync,public-api}.test.ts. src/index.test.ts can own publicSDK contract. No other original integration files. Production files read-only.
- interfaces worker: src/interfaces sibling tests/support/integration plus original tests/integration/{assistant-hooks,assistant-tui}.test.ts and tests/e2e/{assistant-hooks,assistant-tui,cli,mcp,project-context,setup-terminal}.test.ts. Root install/postgres e2e stay global. Production files read-only.
- controller: tests/architecture, sharedtests/fixtures if needed (coordinate), final handoff/spec/plan corrections, full verification and review. No agent modifies architecture guards or global configs.

Use apply_patch for file edits; mechanical move/rewrite commands allowed but no loss of tests. Characterize existing behavior; do not change production just to manufacture RED. Controller adds RED placement/dependency gate. New tests use independent expected outcomes and useful failure scenarios, no existence-only tests, no tests importing tests. Prefer transferring existing cases into own-file coverage where appropriate, avoid copying broad existing tests.

Report per worker in .superpowers/colocated-tests/<scope>-report.md: full source/test map including justified no-logic exemptions, moved existing cases and new tests, focused commands/output, no production mutations, concerns. Run focused scope tests, controller runs fullsuite once all done. Read testsafety and resource lifecycle before touching.
